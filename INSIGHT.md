# INSIGHT

在没有约束的情况下，AI生成了大量无用的中间信息。
且AI还没有学会关键的方法论，迁移创始人在自己面临的问题到客户的问题的思考方法。
似乎需要一种结构让AI学会它。
所以真正的业务问题还摆在那：基于这批认知工程数据，这家公司具体该怎么升级多租户？ 要回答它，需要的不是方法比较，而是让 LLM 把 B 组的关系网络和演化轨迹作为推理材料而非背景装饰，直接推导出架构决策。

目前生成的咨询图式还是很没有主干和结构，就是一堆原则的堆砌。这个和原始图式数据就很没有条理有很大关系。
原始图式也是AI自己生成的，所以自己学习自己又出现了毛病。

---

## 实验前的预判

> 以下三条是实验执行前的推测。执行后对照看——哪些对了、哪些需要修正。

**预判一：方法可行，但 0.2 引用链是脆弱点**

"理论引导的图式迁移"五步流水线逻辑自洽。0.2（质量维度）引用 `schema-theory.md` 时假设了"理论直接给出质量维度"，而实际上图式理论描述的是运作机制，不是质量好坏。0.2 的正确做法是合并双来源。

**预判二：可追溯性优先于自动化**

先手动跑通再自动化。追溯优先于复现。

**预判三：核心创新是"保留类 vs 需验证类"分类**

不是从图书馆引用的，是量潮原创。对应图式理论的同化/顺应。

## 执行后的确认

**方法被验证有效，且有泛化性。** 三个 domain（think/business/health）跨周合并后均跑到 4+ 分（或注解后可到）：

| domain | 评分 | 瓶颈 |
|--------|------|------|
| think | 4.57 | A-1 覆盖度 |
| business | 4.29 | A-2 灵活性 |
| health | 3.57→预估 4+ | A-3 复杂度 |

每个 domain 的瓶颈不同，不存在万能参数。**迁移方法的通用框架成立，但优化策略因 domain 而异。**

**执行确认了三条预判：**
- 方法可行 ✅（且泛化到 3 domain）
- 追溯优先 ✅（transfer-trace 记录了每次决策）
- 保留类/需验证类是核心创新 ✅（A-2 提升 +3.0）

**修正了一条预判：** 0.2 引用链的脆弱点在执行中被正确绕过——质量框架实际合并了理论和工程双来源，没有死磕 schema-theory.md。

**人机分工已走通，但工具支持度仅 3/10（只有数据接入自动，其余全靠人工）。**

## 遗留问题

| 问题 | 状态 |
|------|------|
| dynamics 命名冲突（vs 图式理论的"生命周期"） | 绕过：重命名为"方法论成熟度"，未从根本上解决 |
| W20 无 think 数据（覆盖度盲区） | 待确认是日志遗漏还是 focus 转移 |
| 需验证类比例 2:6（vs gallery qtconsult 的 4:5） | 待验证比例是否合理 |
| products 零引用 | 已修补 AGENTS.md 和 ROADMAP 原则，执行中仍缺失 |
| 换人走通、客户反馈 | 待 TODO 中执行 |
