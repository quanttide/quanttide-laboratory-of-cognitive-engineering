# ROADMAP

实验室当前缺乏两个根基：**理论根基**（library 中的认知工程理论未被引用）和 **数据根基**（实验产出未建立在原始日志之上）。重写的路线图以此为前提。

```
library（理论）→ 建模参考       data/journal（原始日志）→ 事实输入
       ↓                              ↓
        └──────── lab ──────────┘
                      ↓
                  report（产出）
```

## 方法：理论引导的图式迁移（Theory-Guided Schema Migration）

**核心命题**：给定原始 journal 日志，系统性地产出结构化图式（schema），且过程可追溯、可复现。

### 五步流水线

```
原始 journal 数据
    ↓ ① 数据接入
结构化内部表示（situations / intentions / thoughts）
    ↓ ② 理论对齐
用图式理论（生命周期、同化/顺应）审视原始数据，
标记哪些是"新经验"、哪些是"已有图式的强化"
    ↓ ③ 因果拆解
拆为两类：
  - 保留类（跨组织通用，来源于 CTA 的任务分解经验）
  - 需验证类（依赖客户特定条件，设计准入期验证协议）
    ↓ ④ 结构化
填入 schema 七要素：
  entities / causals / boundaries / properties /
  dynamics / mappings / biases
    ↓ ⑤ 质量评估
用从理论推导的维度（coherence / specificity / bias coverage）打分
```

### 关键设计决策

**1. 理论框架在前，数据在后**

直觉做法：读 journal → 找模式 → 写 schema。但本方法顺序是"先理论对齐，再数据接入"。理论筛子在前，防止"数据说什么就写什么"。

**2. 保留类 vs 需验证类对应图式理论的核心机制**

| 因果链类型 | 对应理论 | 处理方式 |
|-----------|---------|---------|
| 保留类（跨组织通用） | 同化（assimilation）— 新经验融入已有图式 | 直接写入 schema |
| 需验证类（依赖客户条件） | 顺应（accommodation）— 需调整图式以适应新信息 | 设计准入期验证协议，通过才写入 |

**3. 质量评估嵌入流程而非事后补充**

每个产出的 schema 附带一份质量报告——不只"产出了什么"，还有"它有多好、哪里不够好"。

**4. 实践参考：不 reinvent the wheel**

本方法的每一步同时对照 `library/products/index.md` 中已有的开源实践，避免重复造轮子。具体对照见各步骤的"产品参考"列。

## 阶段 0：奠基

**目标**：在理论和数据上建立可追溯的根基。

| 步骤 | 内容 | 理论来源 | 产品参考 |
|------|------|---------|---------|
| 0.1 理论对齐 | 将 lab 的 schema 定义与图式理论（同化/顺应、生命周期）对照，记录差异与设计决策 | `library/theories/schema-theory.md` | `cc-thinking-skills`——它们如何将心智模型编码为 AI 技能 |
| 0.2 质量维度 | 合并双来源：理论可推导 3 维（覆盖度/灵活性/复杂度），工程引入 4 维（内部一致性/外部有效性/任务适用性/可沟通性） | `library/theories/schema-theory.md` + 工程经验 | `model-thinking`——200+ 心智模型的质量保证机制 |
| 0.3 数据接入 | 建立从 `../../data/journal/` 读取 → lab 内部表示（situations、intentions、thoughts）的管道 | 参照 `docs/gallery/AGENTS.md` 的心智模型抽取流程 | — |
| 0.4 基线评估 | 用 0.2 的质量维度评估从 journal 提取的首批 schema | journal 原始数据 | — |

**验证标准**：能输出一份"lab schema 定义 vs 图式理论"的差异报告，以及从 journal 提取的 schema 质量基线分。

## 阶段 1：方法论

**目标**：在理论指导下构建可复现的图式迁移方法论。

| 步骤 | 内容 | 理论来源 | 产品参考 |
|------|------|---------|---------|
| 1.1 迁移框架 | 以阶段 0 的理论对齐为前提，定义迁移的输入/输出/步骤 | 心智模型、CTA 方法 | `depict`——心智模型分享的工具支持 |
| 1.2 映射类型 | 在旧有的 4 种映射类型基础上，验证其与 CTA 的认知任务分解是否一致 | `library/theories/cognitive-task-analysis.md` | — |
| 1.3 验证机制 | 对"需验证"类因果链，设计准入期验证协议 | 分布式认知中的信息流设计 | `agentic-memory`——心理学记忆概念工程化的验证经验 |
| 1.4 第一次迁移 | 从 journal 原始数据出发，按 0.3 的数据管道 + 1.1-1.3 的方法迁移生成 `schema2.yaml` | journal 原始数据 | — |

**验证标准**：从 journal 原始数据 → schema2 的过程可完整追溯，每一步有理论依据或明确的设计决策记录。

## 阶段 2：自动化

**目标**：将方法论编码为可重复执行的程序。

| 步骤 | 内容 | 产品参考 |
|------|------|---------|
| 2.1 数据层 | 实现 journal 原始数据读取、解析、校验模块（复用 `quanttide-think` 数据模型） |
| 2.2 迁移层 | 将迁移步骤编码为可组合的转换函数，支持手动/自动混合执行 |
| 2.3 报告层 | 生成迁移记录（含理论引用、设计决策、分岔点记录），输出到 `reports/` |
| 2.4 闭环验证 | 用阶段 0 的质量标准自动评估产出 schema，对比基线 |

**验证标准**：在输入 journal 数据不变的前提下，两次运行产出相同。
