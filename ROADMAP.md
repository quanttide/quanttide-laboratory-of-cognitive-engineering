# ROADMAP

## 为什么做这些

这套方法的价值不在方法论本身，而在它改变了交付物：

| 内功（团队内部） | → 外功（客户感知） |
|---|---|
| 保留类/需验证类区分 | 每条建议标注可信级——"已验证"或"需验证" |
| 数据可追溯 | 质疑时溯源到 journal 原始记录，不是"凭经验说" |
| 7 维质量评估 | 交付物附带质检报告——知道哪里好、哪里还有缺口 |

## 当前状态

阶段 0（奠基）和阶段 1（方法论）已执行完毕。方法被验证有效（质量 2.86 → 4.14）。当前为阶段 2。

```
library/theories  library/products  data/journal
       ↓                ↓               ↓
        ──────── lab ────────→  report（产出）
```

## 方法概要

**理论引导的图式迁移**——从 journal 原始数据到结构化 schema 的五步流水线：

```
journal → ① 数据接入 → ② 理论审视 → ③ 因果拆解（保留类/需验证类）
       → ④ 结构化填充（7 要素）→ ⑤ 质量评估（7 维度）→ schema + trace
```

人机分工：① 全自动，②-⑤ LLM 辅助 + 人工签注。
两个前置筛子：理论筛（符合认知科学吗？）+ 实践筛（有现成方案吗？）。

## 阶段 2：自动化

**目标**：将手动步骤编码为可重复执行的程序，工具支持度从 3/10 提升至 ≥7/10。

### 优先级

| 优先级 | 步骤 | 原因 | 产品参考 |
|--------|------|------|---------|
| P0 | ④ 结构化填充 | 规则最明确、重复性最高、收益最大 | `cst`——认知架构工具包的模块设计 |
| P1 | ⑤ 质量评估 | 框架已定义，可编码为评分函数 | — |
| P2 | ③ 因果拆解辅助 | 需验证类判定仍需人工，先 LLM 辅助分类 | `Soar`——规则引擎设计 |
| P3 | ① 数据层封装 | 已有 `ingest.py`，集成数据模型做校验 | — |
| P4 | 报告层 | 自动生成迁移记录和追溯 | `Hindsight`——经验追溯机制 |

### 验证标准

- 输入 journal 数据不变，两次运行产出相同
- 工具支持度 ≥7/10
- 新增 domain 的迁移能不写代码完成

### 遗留问题（同步解决）

| 问题 | 解决方式 |
|------|---------|
| dynamics 命名冲突 | 在 schema 元信息中补充 `maturity` 字段，保留原字段名 |
| 需验证类比例 | 执行第二个 domain 迁移，验证 2:6 是否合理 |
| products 零引用 | 每次迁移前先扫 products/index.md（已入 AGENTS.md 工作纪律） |
