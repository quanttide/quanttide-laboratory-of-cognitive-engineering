# ROADMAP

实验室当前缺乏三个根基：**理论根基**（library 中的认知工程理论未被引用）、**实践根基**（library 中的产品实践未被引用）和 **数据根基**（实验产出未建立在原始日志之上）。重写后的路线图已完整执行阶段 0-1，三个根基已建立。阶段 2 为当前工作目标。

```
library/theories（理论参照）  library/products（实践参照）  data/journal（原始日志）
       ↓                              ↓                        ↓
        └────────────────────── lab ──────────────────────┘
                                      ↓
                                 report（产出）
```

> **原则**：每个阶段开始前，先查 `library/products/index.md` 看是否有可参考的开源实践。

## 方法：理论引导的图式迁移（Theory-Guided Schema Migration）

**核心命题**：给定原始 journal 日志，系统性地产出结构化图式（schema），且过程可追溯、可复现。

### 五步流水线

```
原始 journal 数据
    ↓ ① 数据接入
结构化内部表示（situations / intentions / thoughts）
    ↓ ② 理论对齐
用图式理论（生命周期、同化/顺应）审视原始数据，
标记哪些是"新经验"、哪些是"已有图式的强化"
    ↓ ③ 因果拆解
拆为两类：
  - 保留类（跨组织通用，来源于 CTA 的任务分解经验）
  - 需验证类（依赖客户特定条件，设计准入期验证协议）
    ↓ ④ 结构化
填入 schema 七要素：
  entities / causals / boundaries / properties /
  dynamics / mappings / biases
    ↓ ⑤ 质量评估
用从理论推导的维度（coherence / specificity / bias coverage）打分
```

### 关键设计决策

**1. 理论 + 实践在前，数据在后**

直觉做法：读 journal → 找模式 → 写 schema。但本方法顺序是"先查理论和实践，再接入数据"。两个前置筛子：

- **理论筛**：这符合认知科学吗？（source: `library/theories/`）
- **实践筛**：这有现成的工程方案吗？优缺点是什么？（source: `library/products/`）

**2. 保留类 vs 需验证类对应图式理论的核心机制**

| 因果链类型 | 对应理论 | 处理方式 |
|-----------|---------|---------|
| 保留类（跨组织通用） | 同化（assimilation）— 新经验融入已有图式 | 直接写入 schema |
| 需验证类（依赖客户条件） | 顺应（accommodation）— 需调整图式以适应新信息 | 设计准入期验证协议，通过才写入 |

**3. 质量评估嵌入流程而非事后补充**

每个产出的 schema 附带一份质量报告——不只"产出了什么"，还有"它有多好、哪里不够好"。

**4. 实践参考先行**

每个阶段开始前先查 `library/products/index.md`，确认：
- 这个问题别人是否已经解决？有现成方案就不重复造轮子
- 别人的方案有什么优缺点？可以作为设计决策的参考
- 如果没找到对应产品，这是一个值得探索的空白领域

具体对照见各步骤的"产品参考"列。

## ✅ 阶段 0：奠基（已完成）

**目标**：在理论和数据上建立可追溯的根基。
**结果**：全部 4 步执行完毕，7 个产出物已生成。

| 步骤 | 产出 | 验证 |
|------|------|------|
| 0.1 理论对齐 | `docs/alignment-report.md` — 7 字段逐字段对照，发现 dynamics 命名冲突 | ✅ |
| 0.2 质量维度 | `docs/quality-framework.md` — 双来源 7 维度评分框架（理论 3 维 + 工程 4 维） | ✅ |
| 0.3 数据接入 | `src/data/ingest.py` — journal→JSON 管道，5 周 33 domain 全部解析 | ✅ |
| 0.4 基线评估 | `data/baseline-assessment.md` — 9 domain 均分 2.86（待改进） | ✅ |

## ✅ 阶段 1：方法论（已完成）

**目标**：在理论指导下构建可复现的图式迁移方法论。
**结果**：全部 4 步执行完毕，方法被验证有效（质量 2.86 → 4.14，+1.28）。

| 步骤 | 产出 | 验证 |
|------|------|------|
| 1.1 迁移框架 | `docs/transfer-framework.md` — 五步流水线 + 人机分工 + 追溯格式 | ✅ |
| 1.2 + 1.3 映射验证 | `docs/mapping-verification.md` — 4 映射类型 vs CTA 全部一致 + 验证协议模板 | ✅ |
| 1.4 第一次迁移 | `data/journal-schema.yaml` + `data/transfer-trace.md` + `reports/2026-W24-migration.md` | ✅ 总分 4.14（优秀） |

### 阶段 1 遗留问题

| 问题 | 严重度 | 说明 |
|------|--------|------|
| dynamics 命名冲突 | 中 | lab 的 dynamics 描述"领域演化"而非"图式自身演化"，当前绕过方案为重命名 |
| W20 无 think 数据 | 低 | 覆盖度盲区，需确认是日志遗漏还是 focus 转移 |
| 需验证类比例 2:6 | 中 | 首次迁移仅 2 条 verifiable（vs 6 条 retainable），参考 gallery qtconsult 的 4:5 |
| products 零引用 | 中 | 所有产出未引用 products/index.md，已修补 AGENTS.md 和 ROADMAP 原则 |
| 工具支持度 3/10 | 高 | 只有数据接入是自动的，迁移/评估/报告全手动 |

## 🔄 阶段 2：自动化（当前）

**目标**：将方法论编码为可重复执行的程序，提升工具支持度。
**前提**：阶段 1 遗留问题中的"products 零引用"已通过 AGENTS.md 和 ROADMAP 原则修补，其余问题在自动化中同步解决。

| 步骤 | 内容 | 产品参考 |
|------|------|---------|
| 2.1 数据层 | 将 `src/data/ingest.py` 封装为可复用模块，集成 `quanttide-think` 数据模型做校验 | `cst`——认知架构工具包的可组合模块设计 |
| 2.2 迁移层 | 将步骤 ②-④ 编码为可组合的转换函数，支持手动/LLM 辅助/全自动三种模式 | `Soar`——认知架构的规则引擎设计 |
| 2.3 报告层 | 自动生成迁移记录（含理论引用、设计决策、分岔点记录），输出到 `reports/` | `Hindsight`——智能体经验学习和追溯机制 |
| 2.4 闭环验证 | 用阶段 0 的质量标准自动评估产出 schema，对比基线，输出质量变化报告 | — |

### 阶段 2 优先级

1. **自动化步骤 ④ 结构化填充**——规则最明确，重复性最高，收益最大
2. **自动化步骤 ⑤ 质量评估**——框架已定义，可编码为评分函数
3. **自动化步骤 ③ 因果拆解**——需验证类的判定仍需人工，LLM 辅助分类可先实现

**验证标准**：在输入 journal 数据不变的前提下，两次运行产出相同。工具支持度从 3/10 提升至 ≥7/10。
