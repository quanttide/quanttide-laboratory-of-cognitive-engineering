[package]
name = "project-10"
version = "0.1.0"
edition = "2021"
description = "Experiment: motif discovery via concern tracing across all clusters"

[[bin]]
name = "project-10"
path = "src/main.rs"

[dependencies]
qtcloud-think-intent = { path = "../../apps/intent" }
