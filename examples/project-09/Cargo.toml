[package]
name = "project-09"
version = "0.1.0"
edition = "2021"
description = "Experiment: multi-turn GraphRAG scaffold with accumulated DiscoveryState"

[[bin]]
name = "project-09"
path = "src/main.rs"

[dependencies]
intent-llm = { path = "../../packages/rust/intent-llm" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
