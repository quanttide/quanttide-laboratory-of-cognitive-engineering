# 项目三：开发待办

## P0 — 基础设施

- [ ] 初始化 Rust 项目（`cargo init`，配置 `Cargo.toml` 依赖：serde, serde_json, serde_yaml）
- [ ] 定义数据模型（`src/models.rs`：Cluster, KeywordEntry, MatchResult）

## P0 — 关键词表构建

- [ ] 实现 YAML 解析：从 `intent.yaml` 提取 10 个簇的 `id`, `name`, `per_week`
- [ ] 实现分词器：按标点/空格切分，保留双字及以上词
- [ ] 实现停用词过滤："的、了、是、在、有、我、这、也、和、就、都、而、及、与、着、或、一个、没有、我们、他们、因为、所以、但是、可以、这个、那个"等
- [ ] 实现关键词去重 + 序列化 → `data/keywords.json`
- [ ] 验证：确认 `keywords.json` 包含 10 个簇，每个簇有关键词列表

## P1 — 匹配算法

- [ ] 实现文本分词（与关键词表构建使用相同分词器）
- [ ] 实现匹配度计算：`|intersection(文本词, 簇关键词)| / |簇关键词|`
- [ ] 实现阈值判定（先写死 0.1，后续调优）
- [ ] 实现证据追踪：记录命中关键词的原文词
- [ ] 序列化匹配结果 → stdout JSON

## P1 — 阈值调优

- [ ] 准备调优集：从 W19-W23 选取已知 intent 归属的段落（利用已有人工标注）
- [ ] 运行匹配器，记录不同阈值下的正检率 vs 误报率
- [ ] 确定最优阈值（最大化 F1 或根据实验目标选择）

## P2 — CLI

- [ ] 实现 stdin 读取，stdout JSON 输出
- [ ] 支持 `--threshold` 参数覆盖默认阈值
- [ ] 支持 `--keywords` 参数指定关键词表路径

## P2 — 验证

- [ ] 编写测试用例（已知输入 → 预期输出）
- [ ] 测试停用词过滤正确性
- [ ] 测试边界情况：空文本、纯停用词文本、多簇匹配
