# AGETNS

## 必须依赖的项目

- `quanttide-agent`
- `quanttide-think`
- `qtcloud-think-cli`

## 数据输入

- `../../data/journal/`：原始日志，实验的事实输入（只读，如需编辑必须复制到本仓库内）。
- `../../data/library/theories/`：理论参照——每个阶段开始前先查是否有对应理论。
- `../../data/library/products/`：实践参照——每个阶段开始前先查是否有对应开源实践。
- `docs/gallery/`：可选参考，不强制消费。

## 工作纪律

- 每次工作结束以后，推测我的意图并输出。
- 默认执行TODO.md文件，如果没有则根据ROADMAP.md分解，再没有则制作两者再开始。
- 默认一次性执行所有TODO，假设TODO已经经过纠正。
- **每个阶段开始前，先查 `library/products/index.md` 和 `library/theories/index.md`，确认是否有可参考的实践或理论。**
