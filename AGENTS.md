# 探索项目管理规范

## 提交
- 每次提交时必须同时推送到云端（`git push`）

## 项目结构
- **项目-任务两级**：一个项目包含多个任务，任务为最小执行单元。此为**实现/文件组织层面**的层级；**意图本身的层级**（领域关切 / 核心议题 / 单周验证）在方法论中定义，两者不强制对应
- **README 即设计**：每个项目的 `README.md` 记录该项目下所有任务的设计（输入、方法、输出）。但 README 中的规划是**起点假设**，执行中发现的结构有权推翻规划
- **输出即文件**：每个任务产生一个具体文件（`outputs/` 或 `data/formal/`）
- **模型先行**：操作数据前先定义模型（`docs/models.md`）

## 模块组织原则（来自经验教训）
- **按领域概念分组，不按文件格式分组**：YAML/JSON 入口类型归入对应的领域模块，不单独建 `yaml.rs`
- **不被多模块复用的类型不单独抽模块**：`builder.rs`、`relation.rs`、`keyword.rs`、`query.rs` 这类只被一个消费者使用的中间模块是碎片，直接归入消费者
- **一次性拆完，不要半途而废**：类型重组时先想清楚完整的模块结构再动手，避免 `models.rs` 拆一半的尴尬状态
- **Rust 的"数据定义与行为分离"不盲目照搬**：类型定义、构建逻辑、运行时操作如果属于同一领域概念，放在同一个文件里内聚性更高

## 任务流程
1. **设计**：先在 `workflow.md` 中写明输入、方法、输出格式
2. **审核**：用户确认设计后再执行
3. **执行**：按设计产出文件
4. **验收**：用户验收后添加元数据（审核状态、质量评价）
5. **重构**：验收不合格时删除或重做，不在原文件上修补

## 命名规范
- 模型名用英文（Notion / Topic / Intent / Thought）
- 文件名全小写 + 连字符，用单数（`intent-relation.md`）
- 目录按功能组织（`data/raw/` 原始数据，`data/cleaned/` 清洗产物，`data/formal/` 正式数据，`docs/` 文档）
