# 探索项目管理规范

## 提交
- 每次提交时必须同时推送到云端（`git push`）

## 项目结构
- **项目-任务两级**：一个项目包含多个任务，任务为最小执行单元
- **输出即文件**：每个任务产生一个具体文件（`outputs/` 或 `assets/refined/`）
- **模型先行**：操作数据前先定义模型（`docs/models.md`）

## 任务流程
1. **设计**：先在 `workflow.md` 中写明输入、方法、输出格式
2. **审核**：用户确认设计后再执行
3. **执行**：按设计产出文件
4. **验收**：用户验收后添加元数据（审核状态、质量评价）
5. **重构**：验收不合格时删除或重做，不在原文件上修补

## 命名规范
- 模型名用英文（Notion / Topic / Intent / Thought）
- 文件名全小写 + 连字符（`intent-relations.md`）
- 目录按功能组织（`assets/raw/` 原始数据，`assets/refined/` 处理产物，`docs/` 文档）
