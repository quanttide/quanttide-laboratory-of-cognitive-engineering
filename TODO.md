# 实验 ROADMAP: 数据 vs 规则引擎 vs 两者

基于 `data/qtconsult.md` 的真实需求——单租户升级多租户方案与报价。

## 实验设计

| 组别 | 方式 | 工具 |
|------|------|------|
| **对照组 A** | 直接读 journal YAML | serde_yaml 解析原始文件 |
| **实验组 B** | 通过 CLI 接口查询 | `Repo` + `analyze` API |
| **实验组 C** | CLI + LLM 综合 | `Repo` + `analyze` + LLM 生成 |

**验证问题**：
1. 原始数据本身（A）是否足够生成方案？
2. 规则引擎（B 的关系/演化/一致性检测）是否比纯数据更有价值？
3. LLM 综合（C）是否在规则之上进一步增质？

## 验证维度

### 维度一：数据完整性
| 标准 | A（纯数据） | B（规则引擎） | C（+LLM） |
|------|-------------|---------------|-----------|
| 能否提取到相关 domain？ | 需手动遍历 | `Repo::domains()` 自动 | 同 B |
| 能否获取 domain 间关系？ | 需自建解析 | `DomainFile::relations()` | 同 B |
| 能否追踪跨周演化？ | 需自建逻辑 | `analyze::track_evolution()` | 同 B |

### 维度二：分析深度
| 标准 | A | B | C |
|------|---|---|---|
| 数据一致性检查 | ❌ 无 | ✅ `Repo::describe()` | 同 B |
| 冲突检测 | ❌ 无 | ✅ relation_type=conflict | 同 B + 解释 |
| 优先级漂移 | ❌ 无 | ✅ 跨期对比 | 同 B + 建议 |
| 方案生成 | ❌ 无 | ❌ 无 | ✅ LLM 生成 |

### 维度三：交付质量
- A 组输出：原始数据结构列表
- B 组输出：结构化分析报告（含关系、一致性、演化）
- C 组输出：完整技术方案 + 报价

## 实验步骤

### Step 1: 对照组 A — 纯数据访问
- [ ] 用 `serde_yaml` 直接读取 `data/journal/quanttide-founder/2026-W23/*.yaml`
- [ ] 输出：列出所有 domain 的 schemas/situations/intentions
- [ ] 评估：能回答什么问题？不能回答什么问题？

### Step 2: 实验组 B — CLI 接口
- [ ] 用 `qtcloud-think-cli` 的 `Repo` 读取同一份数据
- [ ] 调用 `describe()`、`relations()`、`track_evolution()`
- [ ] 输出：加上了关系网络、一致性状态、演化轨迹
- [ ] 评估：规则引擎增加了什么价值？

### Step 3: 实验组 C — CLI + LLM
- [ ] 将 B 的输出作为上下文，发给 LLM
- [ ] Prompt："基于以下认知工程数据，生成单租户升级多租户的技术方案与报价"
- [ ] 输出：完整的咨询交付物
- [ ] 评估：LLM 综合是否产生了增量价值？

### Step 4: 对比总结
- [ ] A vs B：纯数据 vs 规则引擎，哪个信息更有用？
- [ ] B vs C：结构化数据 vs LLM 综合，哪个产出更可交付？
- [ ] 结论：三者的分工应该是什么？

## 文件

| 文件 | 用途 |
|------|------|
| `src/main.rs` | 三个实验组的入口 |
| `data/qtconsult.md` | 咨询需求输入 |
| `data/report.md` | 最终对比结论 |

## 预期

- A 组输出：原始数据罗列，需要人工理解
- B 组输出：规则处理后的结构化洞察，直接可读
- C 组输出：完整的可交付方案，但可能信息失真
